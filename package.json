{
    "name": "roomkit-monorepo",
    "private": true,
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "typecheck": "turbo run typecheck",
        "test": "turbo run test",
        "clean": "turbo run clean",
        "dev": "turbo run dev"
    },
    "packageManager": "bun@1.2.23",
    "devDependencies": {
        "turbo": "^2.4.0",
        "typescript": "^5.7.0"
    }
}
